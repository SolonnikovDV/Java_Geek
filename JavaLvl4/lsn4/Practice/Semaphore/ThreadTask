package lsn4.homeWork;

public class ThreadTask {

    // менять нельзя!
    public void multiThreadRecord(Thread[] threads) {
        for (int i = 0; i < 3; i++) {
            threads[i].start();
        }
    }

}
